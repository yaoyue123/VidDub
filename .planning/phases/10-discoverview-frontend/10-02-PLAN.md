---
phase: 10-discoverview-frontend
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/VideoCard.vue
autonomous: true
requirements: [UI-02, UI-03]

must_haves:
  truths:
    - "VideoCard component renders a single video as a card with thumbnail, title, channel, view count, duration badge, and publish date"
    - "VideoCard shows a loading skeleton state when thumbnail is loading"
    - "VideoCard shows a fallback placeholder when thumbnail fails to load"
    - "VideoCard emits 'add-to-pipeline' event when the action button is clicked"
    - "VideoCard accepts a DiscoveryItem as its primary prop"
  artifacts:
    - path: "frontend/src/components/VideoCard.vue"
      provides: "Reusable video card component for DiscoverView results grid"
      min_lines: 150
      contains: "defineProps"
  key_links:
    - from: "frontend/src/components/VideoCard.vue"
      to: "frontend/src/api/index.ts"
      via: "imports DiscoveryItem type"
      pattern: "import type.*DiscoveryItem"
---

<objective>
Create a reusable VideoCard component that renders a single YouTube video result as a card with thumbnail, metadata, and an "Add to Pipeline" action button. This component is the atomic unit of the results grid in Plan 10-03.

Purpose: Encapsulate the video card rendering (thumbnail with fallback, metadata formatting, action button) into a single self-contained component that any tab can reuse. Prevents duplication across Search, Keywords, and Channels tabs.
Output: New file frontend/src/components/VideoCard.vue
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/api/index.ts (DiscoveryItem interface at line 528, DiscoveryResponse at line 539)
@frontend/src/views/DiscoverView.vue (existing card styling at lines 362-419 for reference patterns)
@frontend/src/views/DiscoverView.vue (formatViews and formatDuration functions at lines 69-79)
@frontend/src/styles/theme.css (CSS variable names for theming)

The DiscoveryItem interface from api/index.ts:
```typescript
export interface DiscoveryItem {
  youtube_id: string
  title: string
  channel: string
  duration: number
  view_count: number
  like_count: number
  thumbnail_url: string
  youtube_url: string
}
```

Use the existing CSS variable system: --fs-xs, --fs-sm, --space-3, --space-4, --color-text, --color-text-placeholder, --color-bg-page, --color-primary, --color-border, --radius-sm, --radius-md.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VideoCard.vue component</name>
  <files>
    frontend/src/components/VideoCard.vue
  </files>
  <action>
    Create a new Vue 3 SFC at frontend/src/components/VideoCard.vue. This is a pure presentational component with no Pinia dependencies.

    Props:
    - `video` (type: DiscoveryItem, required) — the video data to display
    - `loading` (type: boolean, default: false) — show skeleton placeholder when true
    - `actionLabel` (type: string, default: '加入搬运') — text for the action button
    - `compact` (type: boolean, default: false) — if true, render a smaller variant

    Emits:
    - `add-to-pipeline` — emitted when action button is clicked, no payload needed (caller reads video prop)

    Script section (script setup lang="ts"):
    1. Import `ref` from 'vue' and `Plus` from '@element-plus/icons-vue'
    2. Import `ElButton`, `ElIcon` from 'element-plus'
    3. Import type `DiscoveryItem` from '@/api'
    4. Define props and emit using `defineProps` and `defineEmits`
    5. Create `thumbError` ref (boolean, default false) — set to true when thumbnail fails to load, so fallback is shown
    6. Create helper functions:
       - `formatViews(n: number | null | undefined): string` — formats to K/M notation (e.g., 1500 -> "1.5K"). If null/undefined, return '-'. Pattern: match existing DiscoverView.vue line 69-73.
       - `formatDuration(sec: number | null | undefined): string` — formats to M:SS (e.g., 845 -> "14:05"). If null/undefined, return '-'. Pattern: match existing DiscoverView.vue lines 75-79.
       - `formatDate(dateStr: string | null | undefined): string` — formats to locale date via `new Date(dateStr).toLocaleDateString('zh-CN')`. If null/undefined, return '-'.

    Template:
    Use `<div class="video-card y2b-card">` as root element. Do NOT add cursor-pointer or click handler — leave that behavior to the parent.

    Card structure:
    1. **Thumbnail container** (class="card-thumb"):
       - If `video.thumbnail_url && !thumbError`: render `<img :src="video.thumbnail_url" @error="thumbError = true" loading="lazy" />`
       - Else: render a placeholder div with `<el-icon :size="32"><VideoPlay /></el-icon>`
       - Duration badge: `<span class="duration-badge">{{ formatDuration(video.duration) }}</span>`

    2. **Card body** (class="card-body"):
       - Title: `<h3 class="card-title">{{ video.title }}</h3>` — 2-line clamp with text overflow ellipsis
       - Channel name: `<p class="card-channel">{{ video.channel }}</p>` — single line, muted color
       - Meta row: two items side-by-side
         - Views: `<span>{{ formatViews(video.view_count) }} 播放</span>`
         - Published date: `<span>{{ formatDate(video.published_at) }}</span>` — only shown if component receives it (DiscoveryItem does not have published_at by default, so handle gracefully with v-if)
       - Action button: `<el-button size="small" type="primary" :icon="Plus" @click.stop="$emit('add-to-pipeline')" style="margin-top: 8px; width: 100%">{{ actionLabel }}</el-button>`

    3. **Loading state**: When `loading` is true, render skeleton placeholders (divs with a subtle gray background) instead of real content. Match the card structure but replace text with empty blocks that have a pulsing animation.

    Style section (scoped):
    Reuse the CSS variable patterns from the existing DiscoverView.vue card styles (lines 362-419). Use grid-friendly sizing: the card should work in a `repeat(auto-fill, minmax(260px, 1fr))` grid.

    Key styles:
    - `.video-card`: overflow:hidden, transition for hover lift effect (transform translateY(-2px))
    - `.card-thumb`: aspect-ratio 16/9, position relative, overflow hidden, background var(--color-bg-page)
    - `.card-thumb img`: width 100%, height 100%, object-fit cover
    - `.thumb-placeholder`: center content, color var(--color-text-placeholder)
    - `.duration-badge`: absolute bottom-right, black translucent bg, white text, small font, rounded
    - `.card-body`: padding var(--space-3)
    - `.card-title`: font-size var(--fs-sm), font-weight 600, 2-line clamp, margin 0 0 4px 0
    - `.card-channel`: font-size var(--fs-xs), color var(--color-text-placeholder), margin 0 0 4px 0
    - `.card-meta`: flex row, space-between, font-size var(--fs-xs), color var(--color-text-regular)
    - `.skeleton-placeholder`: animated pulse effect using a CSS animation with a linear gradient shimmer

    Do NOT add any Pinia/API imports. This component is pure presentational.
    Do NOT use fenced code blocks in this action — the above is all prose directive.
  </action>
  <verify>
    <automated>grep -c "defineProps\|defineEmits\|formatViews\|formatDuration" frontend/src/components/VideoCard.vue</automated>
    Expect: at least 4 matches.
  </verify>
  <done>
    VideoCard.vue exists with props (video, loading, actionLabel, compact), emits (add-to-pipeline), thumbnail with fallback, duration badge, formatted metadata, loading skeleton state, and action button. Style uses CSS variable system. No Pinia or API dependencies.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component | Pure presentational. No data fetching. All data arrives via props. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Spoofing | Thumbnail img src | accept | If malicious thumbnail URL is served, it is rendered as an img — no script execution risk; same as existing DiscoverView behavior |
| T-10-SC | Tampering | npm dependencies | accept | No new npm packages added |
</threat_model>

<verification>
- [x] VideoCard.vue created as presentational component
- [x] Props defined: video (DiscoveryItem), loading, actionLabel, compact
- [x] Emits defined: add-to-pipeline
- [x] Thumbnail with error fallback (missing img -> icon placeholder)
- [x] Duration badge positioned over thumbnail
- [x] Metadata row with formatted views and date
- [x] Action button with Plus icon, click emits event
- [x] Loading skeleton state
- [x] No Pinia or API imports
- [x] CSS variable theming
</verification>

<success_criteria>
- VideoCard.vue renders complete video cards in both normal and compact variants
- Thumbnail shows with graceful error handling (fallback icon on load failure)
- All metadata fields are formatted (views as K/M, duration as M:SS, date as locale)
- Action button triggers add-to-pipeline event
- Loading skeleton state renders correctly
- Card works inside the same grid layout as existing DiscoverView
</success_criteria>

<output>
Create `.planning/phases/10-discoverview-frontend/10-02-SUMMARY.md` when done
</output>
